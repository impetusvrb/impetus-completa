'use strict';

/**
 * M1.2 — Logistics Payload Validator
 * Reutiliza mesma lógica do MES validator.
 */

const { validatePayload } = require('../../mes/validators/mesValidator');

module.exports = { validatePayload };
