# Domain: quality

Bounded context industrial (WAVE 5+) com **runtime universal dual-layer** (operações + governação). API interna: `/api/internal/quality-universal` (feature flag `IMPETUS_QUALITY_UNIVERSAL_RUNTIME_ENABLED`). Serviços legados permanecem em `src/services/`.
