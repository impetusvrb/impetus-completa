# Phase Z.7 — KPI Blindness Detection

Detecta blind spots executivo, operacional e gerencial sem auto-remediação.

## API

`/api/internal/kpi-blindness/{status,blindness,report}`

## Flag

`IMPETUS_KPI_BLINDNESS_DETECTION` — default **off**

## Testes

```bash
npm run test:kpi-blindness
```
