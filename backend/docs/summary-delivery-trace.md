# Summary Delivery Trace

Pipeline `/dashboard/smart-summary`:

1. smartSummaryService.buildSmartSummary  
2. cognitiveGovernance (deny/align)  
3. precision / rollout / Z.8 convergence / Z.9 activation  

Auditoria: `summary_delivery_audit.cross_domain_hints` — narrativa SST ou executiva em domínio qualidade/RH (observability, sem reescrita).
