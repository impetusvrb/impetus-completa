# Safety Domain Specialization (Z.25)

Domínio `safety` no registry Z.24 passa a `maturity: native`, `cockpit_ready: true`.

Perfis piloto: `coordinator_safety`, `supervisor_safety`, `manager_safety`, `safety_technician`, `technician_safety`.

Blocos oficiais: `sst.incident_intelligence`, `sst.permit_governance`, `sst.ppe_compliance`, `sst.hazard_heatmap`, `sst.field_occurrences`, `sst.risk_matrix`, `sst.safety_telemetry`, `sst.safety_narrative`, `sst.safety_ai`.

Isolamento: negado bleed de `quality_spc`, `esg_reporting`, pipelines executivos.
