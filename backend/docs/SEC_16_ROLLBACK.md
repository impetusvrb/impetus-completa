# SEC-16 — Rollback

```env
SECURITY_THREAT_DECEPTION=false
```

```bash
pm2 restart impetus-backend --update-env
```

SEC-01→SEC-15 permanecem inalterados. Nenhum honeypot exposto é removido (nunca foram criados).

Nunca activar automaticamente.
