# KPI Governance Validation (Z.17)

Bloqueia leakage em coordenação:

- faturamento, lucro, EBITDA, margem, OEE executivo, ESG estratégico, score corporativo

Reutiliza `isExecutiveKpi` de Z.16.

Endpoint: `GET /api/internal/operational-validation/kpis`
