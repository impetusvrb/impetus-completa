# Underdelivery Risk Validation (Z.17)

Garante runtime útil sem fabricar dados:

- dashboard não vazio
- cockpit preservado
- KPI mínimo
- summary útil

Riscos: `low` | `medium` | `high`

Nunca `fabricated_data: true`.

Endpoint: `GET /api/internal/operational-validation/underdelivery`
