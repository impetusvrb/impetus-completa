# Quality — Audience Runtime

- **Resolver:** `qualityAudienceNavigation.js` — `resolveQualityAudienceBand(user)` mapeia perfil/contexto para `operator | coordinator | director | auditor | production`.
- **UX:** `resolveQualityUxDensity(band)` → atributos `data-quality-ux` / `data-quality-audience` no workspace (sem forks visuais duplicados).
