# Summary Governance Validation (Z.17)

Valida **antes** da composição (alinha Z.16):

- SST em texto de Qualidade → `summary_cross_domain_detected`
- RH em SST → bleed
- genericity fallback / stale narrative reuse

Endpoint: `GET /api/internal/operational-validation/summaries`
