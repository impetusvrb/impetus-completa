# ============================================================================
# IMPETUS — ARQUIVOS IGNORADOS PELO GIT
# NUNCA commite credenciais, dependências ou artefactos de build locais.
# ============================================================================

# Google Cloud TTS — chave de serviço (nunca commitar)
backend/config/google-tts.json
impetus_complete/backend/config/google-tts.json

# Variáveis de ambiente (CONTÉM SECRETOS — CRÍTICO)
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local
.env.production
backend/.env
frontend/.env
**/.env
!**/.env.example
# Agentes / lab local (tokens gerados em runtime)
backend/.env.edge-agent
backend/.env.edge-agent.*
# Backups de .env
.env.bkp*
.env.backup*
.env.*.bak
**/.env.bkp*
**/.env.backup*
**/.env.*.bak
.env.bak*
**/.env.bak*

# Lab industrial OPC-UA (gerado localmente)
backend/.opcua-lab-endpoint.txt
backend/.opcua-lab-nodes.json
backend/.opcua-lab-*

# Chaves e credenciais
*.pem
*.key
*.p12
*.pfx
*secret*.json
*credentials*.json
service-account*.json
google-services.json
secrets/

# Dependências (principal causa de “excesso de alterações” no IDE)
node_modules/
**/node_modules/
vendor/
**/vendor/
bower_components/

# Build e distribuição
dist/
build/
**/dist/
**/build/
*.min.js
.next/
out/
.nuxt/
.parcel-cache/

# Backend — artefactos
backend/dist/

# Frontend — build de produção e snapshots locais
frontend/dist/
frontend/dist.prev.*/
frontend/dist_backup_*/
admin-portal/dist/

# Deploy / restart backups locais (centenas de MB — nunca versionar)
deploy_backups/
**/deploy_backups/

# Backups locais e dados de runtime
backups/
backend/backups/
backend/data/
backend/logs/

# Logs e temporários
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/
tmp/
temp/
*.tmp
impetus_complete/backend/scripts/test-voz-out/
backend/scripts/test-voz-out/

# Uploads (dados de utilizadores)
uploads/
**/uploads/

# Memória cognitiva persistente local
backend/storage/cognitive-memory.json
backend/storage/cognitive-memory.json.tmp

# Python (lipsync / scripts lab)
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
lipsync/.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Wav2Lip — pesos grandes
lipsync/Wav2Lip/checkpoints/*.pth
lipsync/Wav2Lip/face_detection/detection/sfd/*.pth

# IDE e editores
.idea/
.vscode/
*.swp
*.swo
*~

# Sistema operacional
.DS_Store
Thumbs.db
desktop.ini

# Cache
.cache/
*.tsbuildinfo
.eslintcache

# Testes e cobertura
coverage/
.nyc_output/
*.lcov

# Docker volumes locais
.docker/

# Espelho legado (não usar como base de produção — ver .cursor/rules)
impetus_complete/
**/backend (1)/
**/frontend (1)/

# PM2 / runtime dumps locais (se existirem na raiz)
.pm2/
pids/
