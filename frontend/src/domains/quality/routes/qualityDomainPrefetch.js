/**
 * WAVE 6 — âncora de chunk `domain-quality` para preload / shadow (sem montar UI).
 */
import '../operational-runtime/qualityOperationalFeatureFlags.js';

export default function QualityDomainPrefetch() {
  return null;
}
