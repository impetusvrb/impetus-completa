# Bloco 6 — gitleaks (pre-commit / CI)
title = "IMPETUS gitleaks"

[extend]
useDefault = true

[allowlist]
paths = [
  '''\.env\.example$''',
  '''\.env\.production\.example$''',
  '''backend/docs/''',
  '''\.md$''',
  '''package-lock\.json$'''
]
